// Int
let x: int = 10;

// Float
let height: float = 10.0;

// Boolean
let isNull: bool = true;

// String
let name: string = "ten";

// Char
let character: char = 'c';

// Unit
let unitTest: unit = ();

// Option
let opt: option(int) = Some(10);

// Tuple
let tuple: (int, string) = (10, "ten");

// List
let l: list(int) = [1, 2, 3];

// Array
let arr: array(int) = [|1, 2, 3|];

// Functions
let func: (int, int) => int = (a, b) => a + b;
